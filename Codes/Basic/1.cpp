#include<iostream>//#include <iostream> is a header file library that lets us work with input and output objects,
using namespace std;
//using namespace std means that we can use names for objects and variables from the standard library.
int main(){

    cout << "Hare Krishna" << endl;
//The cout object, together with the << operator, is used to output values and print text.

// Just remember to surround the text with double quotes (""):
    return 0;
}